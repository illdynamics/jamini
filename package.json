{
  "name": "jamini",
  "version": "0.2.3",
  "description": "Production-grade Gemini CLI drop-in replacement that routes to DeepSeek V4 models",
  "type": "module",
  "bin": {
    "jamini": "./bin/jamini.js"
  },
  "main": "dist/cli.js",
  "files": [
    "bin/",
    "dist/",
    "bridge/dist/",
    "bridge/package.json",
    "bridge/tsconfig.json",
    "config/",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "npm run build:bridge && npm run build:cli",
    "build:bridge": "cd bridge && npx tsc",
    "build:cli": "npx tsc -p tsconfig.json",
    "postinstall": "cd bridge && npm install --no-audit --no-fund 2>/dev/null || true",
    "start": "node dist/cli.js",
    "dev": "npx tsx src/cli.ts",
    "test": "npx vitest run",
    "test:watch": "npx vitest",
    "lint": "npx eslint src/ bridge/src/ test/ bridge/test/ --ext .ts --max-warnings 50",
    "typecheck": "npx tsc -p tsconfig.json --noEmit && cd bridge && npx tsc --noEmit",
    "preflight": "npm run build && npm run typecheck && npm run lint && npm test && npm pack --dry-run && npm run check:release-hygiene",
    "clean": "rm -rf dist bridge/dist coverage",
    "test:ci": "npx vitest run --exclude 'test/docker*'",
    "test:unit": "npx vitest run --exclude \"test/docker*\"",
    "check:release-hygiene": "bash scripts/check-release-hygiene.sh",
    "test:real-gemini": "JAMINI_RUN_REAL_GEMINI_TESTS=1 npx vitest run test/real-gemini-cli.integration.test.ts 2>/dev/null || echo \"Real Gemini CLI tests require interactive terminal. Set JAMINI_RUN_REAL_GEMINI_TESTS=1 to enable.\"",
    "privacy:audit": "bash scripts/privacy-audit.sh",
    "test:privacy": "npx vitest run test/privacy.test.ts test/bridge-privacy.test.ts"
  },
  "keywords": [
    "gemini-cli",
    "deepseek",
    "cli",
    "ai",
    "coding-agent",
    "jamini"
  ],
  "license": "Apache-2.0",
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "@google/gemini-cli": "0.42.0",
    "axios": "1.16.0",
    "cors": "2.8.6",
    "dotenv": "16.6.1",
    "express": "5.2.1",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@types/express": "4.17.25",
    "@types/node": "20.19.0",
    "@typescript-eslint/eslint-plugin": "8.59.3",
    "@typescript-eslint/parser": "8.59.3",
    "eslint": "9.39.4",
    "tsx": "4.21.0",
    "typescript": "^5.9.0",
    "vitest": "3.2.4"
  }
}
